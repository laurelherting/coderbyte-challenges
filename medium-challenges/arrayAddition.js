const assert = require('assert');

const arrayAddition = arr => {
  let max = Math.max.apply();
  let x = arr.length;

  return true;


  return false;
};

const a1 = '';
const t1 = 'arrayAddition()';

assert(t1, a1);
