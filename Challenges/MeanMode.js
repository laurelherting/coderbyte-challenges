const assert = require('assert');

function meanMode (arr) {
  const modeMap = {};
  const mode = 0;
  const modeCount = 0;
  const sum = 0;

  for (let i = 0; i < arr.length; i++) {

  }
}

const t1 = meanMode();
assert.strictEqual(t1);
